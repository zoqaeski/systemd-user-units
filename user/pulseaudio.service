[Unit]
Description=PulseAudio Sound System
Before=sound.target
Requires=dbus.socket

[Service]
Type=forking
BusName=org.pulseaudio.Server
ExecStart=/usr/bin/pulseaudio --start --daemonize --fail -v
ExecStop=/usr/bin/pulseaudio --kill
Restart=always
RestartSec=1

[Install]
WantedBy=default.target
